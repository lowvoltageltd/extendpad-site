@AGENTS.md

Publishing (pushing to `master`, creating releases) is public and needs the owner's explicit go-ahead. Preview locally with `python3 -m http.server 8765` in this folder; the embedded browser pane mispaints after scrolling, so use a tall viewport to check long pages.
